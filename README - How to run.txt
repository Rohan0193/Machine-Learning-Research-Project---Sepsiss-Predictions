To run the jupyter notebook, simply press the kernel tab on the top, and select 'restart and run all'

Otherwise you can also run each cell individually from top to bottom. Just ensure that it is ordered and you run each cell from top to bottom.